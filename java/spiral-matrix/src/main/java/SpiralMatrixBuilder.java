public class SpiralMatrixBuilder {


    public int[][] buildMatrixOfSize(int size){

        int output[][] = new int[size][size];
        int columnMin = 0; int columnMax = size -1;
        int rowMin = 0; int rowMax = size - 1;

        for(int i = rowMin; rowMin < rowMax; rowMin++){
            for(int j = columnMin; columnMin < columnMax; columnMin++){

            }
        }


    }
}